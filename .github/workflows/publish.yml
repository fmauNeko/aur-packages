name: publish

on:
  push:
    branches:
      - main
    paths:
      - "*/PKGBUILD"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Find updated package
        id: find_pkgbuild
        run: |
          #!/usr/bin/env bash
          set -euxo pipefail

          echo "pkgbuild=$(git diff --name-only HEAD HEAD~1 "*/.SRCINFO" | head -1 | xargs dirname)" >> $GITHUB_OUTPUT

      - name: Publish package
        uses: KSXGitHub/github-actions-deploy-aur@v4.1.1
        if: ${{ steps.find_pkgbuild.outputs.pkgbuild != '' }}
        with:
          pkgname: ${{ steps.find_pkgbuild.outputs.pkgbuild }}
          pkgbuild: ${{ steps.find_pkgbuild.outputs.pkgbuild }}/PKGBUILD
          commit_username: ${{ secrets.AUR_USERNAME }}
          commit_email: ${{ secrets.AUR_EMAIL }}
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
